# リアクションテーブル
* ID(主キー)
* ユーザーID(外部キー)
* リアクションタイプID(外部キー)
* 投稿ID(外部キー、NULL可)
  * 投稿ID、もしくはリプライIDのどちらかに値が入り、どちらかがNULLでなければならない
* リプライID（外部キー、NULL可）
  * 投稿ID、もしくはリプライIDのどちらかに値が入り、どちらかがNULLでなければならない
* リアクション日時